services:
  db:
    image: postgres:18
    volumes:
      - pg_data:/var/lib/postgresql
    environment:
      - POSTGRES_DB=nisdb
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
    ports:
      - "5433:5432"

volumes:
  pg_data: